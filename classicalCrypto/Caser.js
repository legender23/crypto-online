(function() {
    let Caser = {
        mod26 : (p) => {
            let text = "abcdefghijklmnopqrstuvwxyz";
            let c = [];
            for ( let val of p ) {
                c.push( text.indexOf(val)+text[] );
            }
        }
    }
}();
